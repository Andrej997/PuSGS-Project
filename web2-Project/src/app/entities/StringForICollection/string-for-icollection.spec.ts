import { StringForICollection } from './string-for-icollection';

describe('StringForICollection', () => {
  it('should create an instance', () => {
    expect(new StringForICollection()).toBeTruthy();
  });
});
