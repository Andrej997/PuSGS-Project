import { DoubleForICollection } from './double-for-icollection';

describe('DoubleForICollection', () => {
  it('should create an instance', () => {
    expect(new DoubleForICollection()).toBeTruthy();
  });
});
