import { FastFlightReservation } from './fast-flight-reservation';

describe('FastFlightReservation', () => {
  it('should create an instance', () => {
    expect(new FastFlightReservation()).toBeTruthy();
  });
});
